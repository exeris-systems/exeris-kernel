<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>eu.exeris</groupId>
        <artifactId>exeris-kernel-parent</artifactId>
        <version>0.11.0</version>
        <relativePath>../exeris-kernel-parent/pom.xml</relativePath>
    </parent>
    <artifactId>exeris-kernel-community-kafka</artifactId>

    <name>Exeris Kernel :: Community :: Kafka</name>
    <description>
        Community Kafka / Redpanda EventEngine binding (since 0.7.0).
        Isolates kafka-clients (and its transitives — zstd-jni, lz4-java, snappy-java,
        jackson-databind) so single-node operators of exeris-kernel-community do NOT
        ship a Kafka client on their classpath. See ADR-008 (Open-Core boundary) and
        v0.7 EVENT-204.
    </description>

    <properties>
        <includedGroups/>
        <!--
            Integration tests with Testcontainers Kafka run only when explicitly enabled:
              mvn -pl exeris-kernel-community-kafka test -Dgroups=integration
            Default Surefire runs exclude them so the unit suite stays fast and Docker-free.
        -->
        <excludedGroups>integration,continuity</excludedGroups>

        <!--
            Coverage floor for the Kafka bundle (unit suite only; the Testcontainers ITs run in the
            kafka-integration-gate CI job with a separate jacoco.exec, NOT merged into this floor).
            RATCHETED UP for v0.10 (ADR-049 event-log binding + its unit tests): the added
            KafkaEventLogCodec / KafkaEventStream / KafkaEventStreamAppender / KafkaEventLogAppendFailedEvent
            unit tests lifted the unit-suite measurement to ~40% line / ~23% branch (2026-07-01:
            248/613 line, 38/168 branch). Floors set a few pp below measured — block regressions and
            step toward the 1.0 goal of >=80%. The remaining gap is the broker-bound KafkaEventLog /
            KafkaEventStreamReader, covered by the Testcontainers TCK (not this floor).
        -->
        <jacoco.line.minimum>0.35</jacoco.line.minimum>
        <jacoco.branch.minimum>0.18</jacoco.branch.minimum>
    </properties>

    <dependencies>
        <!-- SPI contracts (The Wall) -->
        <dependency>
            <groupId>eu.exeris</groupId>
            <artifactId>exeris-kernel-spi</artifactId>
        </dependency>
        <!-- Core orchestration (OutboxBrokerPort, OutboxOrchestrator). -->
        <dependency>
            <groupId>eu.exeris</groupId>
            <artifactId>exeris-kernel-core</artifactId>
        </dependency>
        <!-- Community: provider+engine wiring; Community-internal helpers -->
        <dependency>
            <groupId>eu.exeris</groupId>
            <artifactId>exeris-kernel-community</artifactId>
        </dependency>

        <!-- The Kafka client itself — Community-Kafka-internal, not exposed to single-node operators. -->
        <dependency>
            <groupId>org.apache.kafka</groupId>
            <artifactId>kafka-clients</artifactId>
        </dependency>

        <!-- Logging surface kept consistent with other Community modules. -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>

        <!-- TCK abstract suites for binding parity -->
        <dependency>
            <groupId>eu.exeris</groupId>
            <artifactId>exeris-kernel-tck</artifactId>
            <classifier>tests</classifier>
            <type>test-jar</type>
            <scope>test</scope>
        </dependency>

        <!-- Test framework -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Testcontainers Kafka 3.x for integration tests (only run with -Dgroups=integration) -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>kafka</artifactId>
            <scope>test</scope>
        </dependency>

        <!--
            Postgres for cross-engine choreography ITs (Sprint 6c — DIST-302 closure):
            two FlowEngines share a JdbcFlowSnapshotStore via the same Hikari pool, so a
            wake event arriving at the engine that did NOT park the saga can fall back to
            the durable snapshot store and resume it.
        -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
                <configuration>
                    <argLine>@{coverage.argLine} ${jvm.args}</argLine>
                    <groups>${includedGroups}</groups>
                    <excludedGroups>${excludedGroups}</excludedGroups>
                    <!--
                        v0.8 Sprint 6 (Coverage C-P0-02): pick up *IT.java in addition to the
                        Surefire defaults so the kafka-integration-gate CI job (which calls
                        `-DincludedGroups=integration -DexcludedGroups=` against this module)
                        actually discovers CommunityKafkaEventEngineTckIT /
                        CommunityKafkaFlowChoreographyTckIT / CommunityCrossEngineChoreographyIT.
                        Default Surefire 3.2.5 includes only *Test.java / *Tests.java / Test*.java /
                        *TestCase.java; the three IT files end in IT and were silently dead
                        before this include addition. Default tag-exclusion still skips them
                        on `mvn test` without the integration group override.
                    -->
                    <includes>
                        <include>**/Test*.java</include>
                        <include>**/*Test.java</include>
                        <include>**/*Tests.java</include>
                        <include>**/*TestCase.java</include>
                        <include>**/*IT.java</include>
                    </includes>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
