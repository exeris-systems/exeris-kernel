Change under review — `exeris-kernel-core`.

One Javadoc sentence in `FlowSnapshotValidator` loses a doubled word:

```diff
- * The validator refuses a cursor that that points past the last recorded step.
+ * The validator refuses a cursor that points past the last recorded step.
```

No signature, no behaviour and no test changed.
