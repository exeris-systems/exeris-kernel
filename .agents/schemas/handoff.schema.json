{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Handoff (exeris-kernel)",
  "description": "The bundle's shape, narrowed to this repository's roles. `blocking` stays required: the difference between a handoff that may proceed in parallel and one that may not is the only thing a receiving role cannot infer. The base refuses nothing on its own, so the root closer here is this repository's; the object carries no array of objects, so it is the only one.",
  "allOf": [
    {
      "$ref": "../vendor/exeris-agents-2.1.0/schemas/handoff.base.schema.json"
    },
    {
      "properties": {
        "from": {
          "enum": [
            "exeris-router",
            "exeris-architect",
            "exeris-implementer",
            "exeris-tck",
            "exeris-performance",
            "exeris-docs-adr",
            "exeris-evaluator"
          ]
        },
        "to": {
          "enum": [
            "exeris-router",
            "exeris-architect",
            "exeris-implementer",
            "exeris-tck",
            "exeris-performance",
            "exeris-docs-adr",
            "exeris-evaluator"
          ]
        }
      }
    }
  ],
  "unevaluatedProperties": false
}
