# SPI stability surfaces — machine-readable mirror of docs/stability-matrix.md
#
# This file is the gate's copy of the maturity labels declared in the matrix.
# The matrix is the human-facing source of truth; when a surface changes level
# there, update this file in the same commit. `spi-api-diff.sh --verify-surfaces`
# fails if a package is present in the SPI tree but absent from every list here,
# so a new SPI package cannot silently escape classification.
#
# Format: <level>=<comma-separated japicmp include expressions>
# Expressions are package names or fully-qualified class names; japicmp matches
# a package prefix on packages and exactly on classes.
#
# Gate semantics (see docs/stability-matrix.md §Semver policy):
#   stable       — a binary- OR source-incompatible change FAILS the build. Both, because
#                  adding an abstract method to an interface is binary-compatible and
#                  breaks every implementor at invoke time; only the source check sees it.
#   preview      — reported in the release diff, never fails the build.
#   experimental — reported, never fails; no guarantees in any version.
#   internal     — excluded from the consumer-facing report entirely.
#
# A `mixed` package must list EVERY one of its classes. japicmp only compares what
# an include expression selects, so a class named in no list is neither gated nor
# reported — a silent hole, not a missing warning. `--verify-surfaces` checks per
# fully-qualified class name precisely to make that impossible; do not "fix" a
# report from it by widening a class entry to its package.
#
# Deliberate asymmetry — `spi.exceptions` is stable in full, including the
# `…exceptions.crypto` / `.security` / `.scheduling` / `.storage` subtrees, even
# though those domains are themselves `preview`. The Glass-Box error taxonomy
# (code + rawArgs layout) is one contract regardless of how settled the subsystem
# behind it is: an operator parses `EX-CRYPTO-*` the same way whether or not
# `CryptoEngine` is still moving. Stability of the failure vocabulary is not
# stability of the API that raises it.

stable=\
eu.exeris.kernel.spi.diagnostics,\
eu.exeris.kernel.spi.persistence,\
eu.exeris.kernel.spi.flow,\
eu.exeris.kernel.spi.memory,\
eu.exeris.kernel.spi.transport,\
eu.exeris.kernel.spi.exceptions,\
eu.exeris.kernel.spi.telemetry,\
eu.exeris.kernel.spi.bootstrap,\
eu.exeris.kernel.spi.context,\
eu.exeris.kernel.spi.config,\
eu.exeris.kernel.spi.http.HttpClientEngine,\
eu.exeris.kernel.spi.http.HttpServerEngine,\
eu.exeris.kernel.spi.http.HttpProvider,\
eu.exeris.kernel.spi.http.HttpExchange,\
eu.exeris.kernel.spi.http.HttpHandler,\
eu.exeris.kernel.spi.http.HttpClientRequestEnricher,\
eu.exeris.kernel.spi.http.HttpKernelProviders,\
eu.exeris.kernel.spi.http.HttpConfig,\
eu.exeris.kernel.spi.http.HttpMode,\
eu.exeris.kernel.spi.http.HttpRequest,\
eu.exeris.kernel.spi.http.HttpResponse,\
eu.exeris.kernel.spi.http.HttpTypedResponse,\
eu.exeris.kernel.spi.http.HttpStatus,\
eu.exeris.kernel.spi.http.HttpMethod,\
eu.exeris.kernel.spi.http.HttpVersion,\
eu.exeris.kernel.spi.http.HttpHeader

preview=\
eu.exeris.kernel.spi.events,\
eu.exeris.kernel.spi.graph,\
eu.exeris.kernel.spi.security,\
eu.exeris.kernel.spi.crypto,\
eu.exeris.kernel.spi.scheduling,\
eu.exeris.kernel.spi.storage,\
eu.exeris.kernel.spi.http.HttpRequestBodyEncoder,\
eu.exeris.kernel.spi.http.HttpRequestBodyDecoder,\
eu.exeris.kernel.spi.http.HttpResponseBodyDecoder,\
eu.exeris.kernel.spi.http.HttpResponseBodyEncoder,\
eu.exeris.kernel.spi.http.HttpRequestBodyEncoderRegistry,\
eu.exeris.kernel.spi.http.HttpRequestBodyDecoderRegistry,\
eu.exeris.kernel.spi.http.HttpResponseBodyEncoderRegistry,\
eu.exeris.kernel.spi.http.HttpResponseBodyDecoderRegistry,\
eu.exeris.kernel.spi.http.HttpRequestEncodingContext,\
eu.exeris.kernel.spi.http.HttpRequestDecodingContext,\
eu.exeris.kernel.spi.http.HttpResponseEncodingContext,\
eu.exeris.kernel.spi.http.HttpResponseDecodingContext,\
eu.exeris.kernel.spi.http.HttpEncodedBody,\
eu.exeris.kernel.spi.http.HttpRetryPolicy,\
eu.exeris.kernel.spi.http.RetryDecision,\
eu.exeris.kernel.spi.http.HttpAttemptOutcome,\
eu.exeris.kernel.spi.http.HttpRoutePolicy,\
eu.exeris.kernel.spi.http.RouteRequirement,\
eu.exeris.kernel.spi.http.HttpStreamExchange,\
eu.exeris.kernel.spi.http.HttpStreamHandler,\
eu.exeris.kernel.spi.http.StreamEvent

experimental=

internal=\
eu.exeris.kernel.spi.util
