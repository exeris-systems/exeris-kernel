<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>eu.exeris</groupId>
        <artifactId>exeris-kernel-root</artifactId>
        <version>0.5.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>exeris-kernel-parent</artifactId>
    <packaging>pom</packaging>

    <name>Exeris Kernel - Parent</name>
    <description>
        Main Parent POM for Kernel modules.
        Imports the BOM and enforces strict Code Quality gates (Checkstyle, PMD).
    </description>

    <properties>
        <!-- JMH version — mirrored here from BOM so annotationProcessorPaths
             in child modules can reference ${jmh.version} without the BOM
             properties scope limitation. -->
        <jmh.version>1.37</jmh.version>
        <exec.maven.plugin.version>3.5.0</exec.maven.plugin.version>

        <!-- Benchmark parameters — all overridable on CLI with -D -->
        <benchmark.includes>.*</benchmark.includes>
        <benchmark.forks>1</benchmark.forks>
        <benchmark.warmup>3</benchmark.warmup>
        <benchmark.iterations>5</benchmark.iterations>
        <benchmark.output>${project.build.directory}/jmh-results.json</benchmark.output>
        <!-- Default: benchmarks are skipped. Modules that contain @Benchmark classes
             must override this to false in their own pom.xml <properties>. -->
        <benchmark.skip>true</benchmark.skip>
        <!-- Default empty; override in native-dependent modules or profiles. -->
        <native.lib.dir/>
    </properties>

    <!-- =====================================================================
         benchmarks profile — runs JMH microbenchmarks in the activated module.

         Usage (single module):
           mvn verify -P benchmarks -pl exeris-kernel-core
           mvn verify -P benchmarks -pl exeris-kernel-community

         Usage (all modules at once):
           mvn verify -P benchmarks

         Filtering (regex matched against JMH benchmark name):
           mvn verify -P benchmarks -pl exeris-kernel-core "-Dbenchmark.includes=CoreJfrTelemetry"
           mvn verify -P benchmarks -pl exeris-kernel-core "-Dbenchmark.includes=emitMemory"

         Tuning:
           -Dbenchmark.forks=2       JVM forks per benchmark (default: 1)
           -Dbenchmark.warmup=5      warmup iterations       (default: 3)
           -Dbenchmark.iterations=10 measurement iterations  (default: 5)

         CI note:
           NOT bound to the default lifecycle — only runs with explicit -P benchmarks.
           Results are written to target/jmh-results.json for CI ingestion.
         ===================================================================== -->
    <profiles>
        <profile>
            <id>benchmarks</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <version>${exec.maven.plugin.version}</version>
                        <executions>
                            <execution>
                                <id>run-benchmarks</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <skip>${benchmark.skip}</skip>
                                    <executable>java</executable>
                                    <classpathScope>test</classpathScope>
                                    <arguments>
                                        <argument>--enable-preview</argument>
                                        <argument>-XX:+UnlockExperimentalVMOptions</argument>
                                        <argument>-XX:+UseZGC</argument>
                                        <argument>--enable-native-access=ALL-UNNAMED</argument>
                                        <argument>-Djava.library.path=${native.lib.dir}</argument>
                                        <argument>-classpath</argument>
                                        <classpath/>
                                        <argument>org.openjdk.jmh.Main</argument>
                                        <argument>${benchmark.includes}</argument>
                                        <argument>-f</argument>
                                        <argument>${benchmark.forks}</argument>
                                        <argument>-wi</argument>
                                        <argument>${benchmark.warmup}</argument>
                                        <argument>-i</argument>
                                        <argument>${benchmark.iterations}</argument>
                                        <argument>-rf</argument>
                                        <argument>json</argument>
                                        <argument>-rff</argument>
                                        <argument>${benchmark.output}</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>eu.exeris</groupId>
                <artifactId>exeris-kernel-bom</artifactId>
                <version>${project.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-pmd-plugin</artifactId>
                <version>3.28.0</version>
                <dependencies>
                    <dependency>
                        <groupId>net.sourceforge.pmd</groupId>
                        <artifactId>pmd-core</artifactId>
                        <version>7.22.0</version>
                    </dependency>
                    <dependency>
                        <groupId>net.sourceforge.pmd</groupId>
                        <artifactId>pmd-java</artifactId>
                        <version>7.22.0</version>
                    </dependency>
                    <dependency>
                        <groupId>eu.exeris</groupId>
                        <artifactId>exeris-kernel-build-config</artifactId>
                        <version>${project.version}</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <rulesets>
                        <ruleset>pmd-ruleset.xml</ruleset>
                    </rulesets>
                    <targetJdk>26</targetJdk>
                    <failOnViolation>true</failOnViolation>
                    <printFailingErrors>true</printFailingErrors>
                </configuration>
                <executions>
                    <execution>
                        <id>verify-complexity</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>3.6.0</version>
                <dependencies>
                    <dependency>
                        <groupId>eu.exeris</groupId>
                        <artifactId>exeris-kernel-build-config</artifactId>
                        <version>${project.version}</version>
                    </dependency>
                    <!-- Checkstyle 13.x is required for record pattern support in instanceof (java:S6878 / Java 21+).
                         Overrides the locally cached 9.3 which pre-dates that feature.
                         Source: https://mvnrepository.com/artifact/com.puppycrawl.tools/checkstyle -->
                    <dependency>
                        <groupId>com.puppycrawl.tools</groupId>
                        <artifactId>checkstyle</artifactId>
                        <version>13.2.0</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <configLocation>checkstyle.xml</configLocation>
                    <consoleOutput>true</consoleOutput>
                    <failsOnError>true</failsOnError>
                </configuration>
                <executions>
                    <execution>
                        <id>validate-architecture</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>com.mycila</groupId>
                <artifactId>license-maven-plugin</artifactId>
                <version>4.3</version>
                <dependencies>
                    <dependency>
                        <groupId>eu.exeris</groupId>
                        <artifactId>exeris-kernel-build-config</artifactId>
                        <version>${project.version}</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <licenseSets>
                        <licenseSet>
                            <header>license-header.txt</header>
                            <includes>
                                <include>src/**/*.java</include>
                            </includes>
                        </licenseSet>
                    </licenseSets>
                    <mapping>
                        <java>SLASHSTAR_STYLE</java>
                    </mapping>
                </configuration>
                <executions>
                    <execution>
                        <id>check-licenses</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>